import styled from '@emotion/styled'

const StyledFooter = styled.footer`
  text-align: center;
  font-size: 12px;
`

export const Footer = () => {
  return <StyledFooter>Made with &hearts; by Jordan</StyledFooter>
}
